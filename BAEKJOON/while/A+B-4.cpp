#include <iostream>

using namespace std;

int main(){
    int A,B;
    cin.tie(NULL);
    cin.sync_with_stdio(false);
    while(cin >> A >> B) {        
        cout << A + B << "\n";
    } 
    return 0;
}